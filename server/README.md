# Legacy backend

The current Vercel deployment does not use this Express server. The production API lives in `web/api/chat.js` so it can deploy with the Vite app under the configured Vercel Root Directory `web`.
